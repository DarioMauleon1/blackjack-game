import { defineConfig } from "vite";
export default defineConfig({
  base: "/blackjack-game/",
});

// Código creado para que gitpages funciones ya que al tomar como sub directorio /blackjack-game/ no encontraba la ruta del css y js que originalemtne en nuestro código están en el root y no en un sub directorio como creo gitpages
